<div class="col-md-9">
  <div><%= link_to 'New Blog', new_blog_path, class: "btn btn-primary mb-4" if logged_in?(:site_admin) %></div>

   <%= paginate @blogs %>
   <%= render @blogs %>
   <%= paginate @blogs %>
</div>

