<div class="container">

  <div class="card">
        <h2><%= @job.company  %></h2>
        <h3><%= @job.job_title %></h3>
        <p><%= @job.body %></p>
farts
<% if @job.skills.any? %>
  <p>Skills: <% @job.skills.each do |skill| %></p>
      <%= skill.title %>
  <% end %>
<% end %>        
           

        <span class="pull-right">
          <%= link_to fa_icon('pencil-square-o'), edit_job_path(@job) if logged_in?(:site_admin) %>
          <%= link_to fa_icon('trash'), @job, method: :delete, data: { confirm: 'Are you sure you want to delete this post' } if logged_in?(:site_admin) %>
        </span>
        
         <%= link_to "View all job items", jobs_path, class: "btn btn-secondary mt-5" %>
  </div>

</div>

