<div class="container">
<%= @skill.title %>
<% if @skill.jobs.count != 0 %>
  <h4 align="center">Job's containing <%= @skill.title %></h4>

  <% @skill.jobs.each do |job| %>
      <%= job.company %>
  <% end %>
<% else %>
  <h4 align="center">No Jobs yet!</h4>
<% end %>
